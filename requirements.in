# Core dependencies
typer[all]>=0.9.0  # CLI interface with all optional dependencies
moviepy==1.0.3     # Video processing - using exact version to avoid compatibility issues
openai-whisper>=20231117  # Speech recognition
rich>=13.0.0       # Console output formatting

# Additional dependencies that might be needed by the above packages
numpy>=1.22.0       # Required by moviepy and whisper
ffmpeg-python>=0.2.0  # Required for audio processing
decorator>=4.4.2    # Required by moviepy
imageio>=2.31.1     # Required by moviepy
imageio-ffmpeg>=0.4.7  # Required by moviepy for video processing
Pillow>=9.0.0      # Required by moviepy for image processing
proglog>=0.1.10    # Required by moviepy for progress logging
requests>=2.31.0    # Required by moviepy
tqdm>=4.65.0       # Required for progress bars

# Development tools
pip-tools>=7.3.0    # For managing requirements
